# 平台硬化清单（Enterprise-Grade Bar）

> 触发：用户要求 **绝对的可靠、安全、成熟、可观测**。每一步考虑所有边缘情况。能交付企业。
>
> 这份文档是审计 + roadmap。每条标记 P0/P1/P2 + 状态。
> 完成一项划掉。新发现的随时加。

最后更新：2026-04-30（晚 — P1-5 + P1-6）

---

## 评估维度

每条条目需通过 4 道关：

1. **可靠**：所有上游/下游故障模式都被显式处理（NewAPI down / Redis down / DB down / NATS 重投 / 时钟漂移）
2. **安全**：默认拒绝；密钥不进日志；端点鉴权 + 速率限制；CORS/CSP/Cookie 属性正确
3. **成熟**：有运维 runbook；schema 变更可回滚；CHANGELOG；版本化契约；不破坏接入方
4. **可观测**：每个关键操作出 metric + structured log；/readyz 反映依赖；trace span 跨服务

---

## P0 — 立刻修（钱、安全、不可逆）

| ID | 问题 | 维度 | 状态 |
|----|------|------|------|
| P0-1 | Topup→NewAPI 同步无去重，JetStream 重投 → 双扣（用户钱消失） | 可靠 | ✅ 2026-04-30 ship (`f4183c7`) — Redis SETNX + envelope.event_id |
| P0-2 | `idempotency.WebhookDeduper` 默认 fail-open，钱场景应 fail-closed（Redis 挂时丢去重保护） | 可靠 | ✅ 2026-04-30 ship — `WithFailClosed()` + `WithKeyPrefix()` + `ErrRedisUnavailable` 显式契约 |
| P0-3 | newapi_sync 三个操作零 metric，故障率 / 延迟 / 双扣 都看不到 | 可观测 | ✅ 2026-04-30 ship — `newapi_sync_ops_total{op,result}` 12 series；`duplicate` 桶直接计 = 阻止过的双扣次数 |
| P0-4 | `CheckinService.DoCheckin` 服务层 TOCTOU race（read-then-create 中间无锁） | 可靠 | ✅ 2026-04-30 ship — 改 ON CONFLICT DO NOTHING + ErrCheckinAlreadyToday sentinel；并发 20 次连测稳定 |
| P0-5 | `/readyz` 不包含 NewAPI 可达性 — newapi_sync 已挂死也通过健康检查 | 可观测 | ✅ 2026-04-30 ship — soft checker (degraded 字段，不翻 ready=false)；30s 缓存避免每次 /readyz 打 NewAPI |

## P1 — 近期排（影响接入体验、运维成本）

| ID | 问题 | 维度 | 状态 |
|----|------|------|------|
| P1-1 | NewAPI 调用无重试 + 退避，单次抖动就失败 | 可靠 | ✅ 2026-04-30 ship — 3 tries / 指数退避 100ms-1s / ±30% jitter / 仅对 5xx+408+429+net 错误重试 |
| P1-2 | NewAPI 调用无 circuit breaker，故障期持续打死下游 | 可靠 | ✅ 2026-04-30 ship — Closed→Open (5 连失) / 15s 后 HalfOpen 探测 / 探测成功 Close, 失败回 Open |
| P1-3 | CORS allowlist 硬编码 5 个域，新产品要改代码 | 成熟/UX | ✅ 2026-04-30 ship — `CORS_ALLOWED_ORIGINS` env (CSV)；空值回退到历史 5 域；`resolveCORSOrigins` 防御拷贝 |
| P1-4 | 4g 对账 cron 没做 — 漏同步的 account 永久错位 | 可靠 | ✅ 2026-04-30 ship — 5min tick / batch 100 / 幂等借力 OnAccountCreated find-then-create / poison-pill 不阻塞批 / metric 区分 reconcile_tick |
| P1-5 | logout 不让 server 端 invalidate JWT，stolen token 30 天有效 | 安全 | ✅ 2026-04-30 ship — `auth.SessionRevoker` (SHA-256 hash 入 Redis, TTL=剩余 JWT 寿命)；`/auth/logout` 进 revoke 列表；`RequireSession` middleware 每次请求查 revoke 列表；fail-open on Redis error（避免 Redis blip 全员退出登录） |
| P1-6 | `/whoami` `/llm-token` 无 per-user rate limit | 安全 | ✅ 2026-04-30 ship — 新 `handler.RequireSession` 中间件解析 cookie 或 Bearer + 校验 + 注入 `account_id`；路由链改为 `PerIP → RequireSession → PerUser → handler`，PerUser 终于在这两条路由生效 |
| P1-7 | NewAPI calls 无 OpenTelemetry span 透传 | 可观测 | ✅ 2026-04-30 ship — span 名 `newapi.<METHOD path>` + `newapi.attempt` 属性 + 错误状态码 + retry 标记 |
| P1-8 | 没有 CHANGELOG，接入方不知道最近改了啥 | 成熟 | ⬜ |
| P1-9 | `OnAccountCreated` 异步 hook 无 dlq，永久失败的注册 sync 没人发现 | 可靠 | ⬜ |
| P1-10 | 错误响应 JSON 不统一（部分有 `error_code`，部分只有 `error` 文本） | 成熟/UX | 🟡 部分完成 2026-04-30 — `internal_api.go` 两处 UPPERCASE `error_code` 已并入标准 `{error,message}` envelope；`ErrCodeUpstreamFailed` 新增。剩余 17 个 `gin.H{"error": ...}` raw 文本待逐个迁到 `respondError`；rich envelope (`{error: {code,...}}`) 与 flat envelope 共存的取舍待 ADR |

## P2 — 长期 / 架构

| ID | 问题 | 维度 | 状态 |
|----|------|------|------|
| P2-1 | C.2 → C.1 切换条件未明（什么指标触发？） | 成熟 | ⬜ |
| P2-2 | 多 cluster ApplicationSet 同步 apps.yaml 还没落地 | 成熟 | ⬜ |
| P2-3 | 没有混沌测试 / 故障注入回归 | 可靠 | ⬜ |
| P2-4 | 安全加固头（CSP / X-Frame-Options / HSTS）未在 router 层挂 | 安全 | 🟡 部分完成 (`fd59465`) — 5 个头 ship；CSP 暂缓（Vite inline 脚本 nonce 需先规划） |
| P2-5 | 关键密钥（Zitadel PAT / NewAPI admin token）无轮换流程 | 安全 | ⬜ |
| P2-6 | 错误响应里直接暴露内部 err.Error() — 部分场景泄露内部细节 | 安全 | ⬜（grep + sanitize） |
| P2-7 | 数据库迁移没有 down 脚本 / 回滚 runbook | 成熟 | ⬜ |
| P2-8 | 用户协议 + 隐私政策待法务 review 后真正发布 | 成熟 | ⬜ |
| P2-9 | DR 演练（DB 主从切换、跨集群 failover）零次 | 可靠 | ⬜ |
| P2-10 | 没有发布 SLA 指标（/whoami p99、可用率 …） | 成熟 | ⬜ |

---

## 当前 session 范围

只做 **P0-1 + P0-2 + P0-3**（topup 去重 + fail-closed + 三个 metric）。其他不动。

完成验收：
1. `OnTopupCompleted` 接 `eventID` 参数；空 ID → 处理但记 warning
2. Redis SETNX 命中 → no-op + dedup metric +1
3. Redis SETNX miss → 处理；处理失败 → NewAPI 错误传播 NAK，dedup key 自动 TTL 过期允许重试
4. Redis call error → fail-CLOSED（返错让 JetStream NAK + 退避重试，不双扣）
5. 三个 newapi_sync 操作（account_created / topup / llm_token）出 prometheus counter，labels = `op,result`
6. 测试覆盖：dedup 命中 / dedup miss / Redis 错误 / 空 eventID / metric counter 增量

---

## 决策原则（写在硬化清单里防漂移）

- **钱相关 = fail-closed**。出错宁可暂停同步堆积重试，也不允许"反正我也不知道，先扣了再说"
- **接入面任何错误都要带机器可读 code**。`{"error_code": "newapi_sync_disabled", "message": "..."}` 让客户端可分支
- **新加 metric 必须同时加 alert rule**（暂存到 `docs/observability/alerts.md`，下次集中接入 Alertmanager）
- **任何对外行为变更都要更新 CHANGELOG**（P1-8 落地后开始）
