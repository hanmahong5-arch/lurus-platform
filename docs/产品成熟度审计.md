# 产品成熟度审计 — R1 投产准入评估

> 生成时间：2026-04-29
> 触发：用户问"哪个产品达到了上线的成熟度，再投入 R1 生产环境"

## 评估准则

按 Server Landing Policy（CLAUDE.md），R1 (`43.226.46.164`) 准入标准：
1. **已对外商业交付**（有真实付费/活跃用户）
2. CI 全绿、有测试覆盖
3. 生产级 manifest（HPA / PDB / probe）
4. 有 README + 部署文档
5. 无 mock / 无 hardcoded fixture

## 当前 R1 名册

| 服务 | namespace | 用户类型 | 状态 |
|------|-----------|---------|------|
| miaoda-oa | miaoda-oa | 客户（常东松） | ✅ 已交付 |
| newapi | lurus-system | 内部+集成方 | ✅ 稳定运行 |
| www.lurus.cn | lurus-www | 公开访客 | ✅ 营销页 |
| docs.lurus.cn | lurus-system | 公开访客 | ✅ 公开文档 |
| memorus | lurus-system | 内部消费方 | ✅ 仅内调 |
| webgame | lurus-webgame | TBD | 🟡 测试中 |
| platform-core | lurus-platform | **无** | 🟡 持续迭代 |
| lurus-login | lurus-platform | **无** | 🟡 刚上线 UI |
| zitadel | lurus-platform | **无** | 🟡 待启用 |

> 用户确认："现在已上线的产品没有用 Zitadel 的"——上述 ✅ 项均独立运行，不依赖 platform-core 的身份层。

## 在研产品 R1 准入评估

按"成熟度顺序"排列：

### 🥇 Lucrum（候选 R1 升级，但尚不达标）

| 维度 | 现状 | 是否达标 |
|------|-----|---------|
| 用户 | 内测，少量演示账号 | ❌ 无付费 |
| CI | ✅ 全绿，最近 fix register（commit 7c77b1a） | ✅ |
| 测试 | 单元测试齐备，无 e2e | 🟡 |
| Manifest | R6 上有 deployment + service + ingressroute | ✅ |
| 监控 | 基本 health check，无 SLO | 🟡 |
| 文档 | README + plans/ 完备 | ✅ |
| 阻塞 | OIDC client_id 死链未修；多支付通道未完全打通 | ❌ |

**结论**：Lucrum 在 R6 继续迭代。**升级 R1 的前置**：
1. 解 OIDC client_id 死链（路径：迁移到新登录页 cookie 模式，绕开 OIDC，与 platform 简化方向一致）
2. 至少 1 周稳定运行（无 P0 bug）
3. 有付费用户或承诺试用客户

### 🥈 platform-core（按用户指示：留 STAGE 持续迭代）

虽然在 R1 上跑着，但**尚无真实用户依赖**。用户决策："现在没上线的这个 platform 就持续在测试环境迭代"。

不再视作"R1 生产服务"，按 STAGE 标准管理：
- 改动 → 直接 push → 直接 rollout（不需要 freeze window）
- 无需 SLA
- 失败回滚走标准 `kubectl rollout undo`

### 🥉 Tally（未启动）

`2b-svc-psi/Tally` 在规划阶段，无代码上线。NA。

### 桌面产品（不适用）

`2c-gui-switch` / `2c-gui-creator` 是桌面 GUI（Wails），不走 K8s，不在本审计范围。

## 决策

**今天**：
- ✅ 把新登录页部署到 **R1**（因 platform-core 无生产用户，安全）
- 🚫 不主动促销任何产品到 R1

**未来 1-2 周观察**：
- Lucrum 切到 cookie 登录后，跑 1 周观察稳定性
- 若有付费/演示客户承诺使用 → 申请升级 R1（迁移 namespace + ingress 切换）

**长期改造**（与 docs/多租户简化.md 配套）：
- platform-core 把"接入面"从 6 步压成 0 步（drop-in cookie SDK）
- 每个 in-progress 产品都依赖这个简化才能轻松升级 R1

## 升级 STAGE→PROD checklist（参考）

```
□ 1. 7 天 R6 稳定运行无 P0 bug
□ 2. 至少 3 个外部访问者（付费/试用/合作方任意）
□ 3. CI 在最近 5 次 push 全绿
□ 4. 准备好 R1 ingressroute + namespace + RBAC
□ 5. 准备好回滚剧本（"如果挂了，10 分钟内能回 R6"）
□ 6. 通知 oncall 升级窗口
□ 7. 在 R6 标 archive，禁止再写
□ 8. R1 上线 → 跑 24h 观察 → 关 R6 影子
```
